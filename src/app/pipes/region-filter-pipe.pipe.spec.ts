import { RegionFilterPipePipe } from './region-filter-pipe.pipe';

describe('RegionFilterPipePipe', () => {
  it('create an instance', () => {
    const pipe = new RegionFilterPipePipe();
    expect(pipe).toBeTruthy();
  });
});
