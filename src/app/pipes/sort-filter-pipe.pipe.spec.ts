import { SortFilterPipePipe } from './sort-filter-pipe.pipe';

describe('SortFilterPipePipe', () => {
  it('create an instance', () => {
    const pipe = new SortFilterPipePipe();
    expect(pipe).toBeTruthy();
  });
});
